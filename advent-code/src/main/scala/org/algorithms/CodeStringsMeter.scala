package org.algorithms

class CodeStringsMeter {
    def stringLength(codeString: String) = codeString.length

    def codeStringLength(codeString: String) = {
        var index = 1
        while(index < codeString.length) {
            
        }
        index - 1
    }
}
